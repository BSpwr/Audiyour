set(COMPONENT_SRCS "main.c")
set(COMPONENT_ADD_INCLUDEDIRS .)

register_component()
# idf_component_register(SRCS "main.c"
#                     INCLUDE_DIRS ".")

# set_target_properties(${COMPONENT_LIB} PROPERTIES COMPILE_FLAGS -Wno-error=all)